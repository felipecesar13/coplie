{
  "name": "coplie",
  "version": "1.0.0",
  "description": "Linear webhook integration with Copilot CLI",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test --coverage --env-file=.env.test",
    "test:watch": "bun test --watch --coverage --env-file=.env.test",
    "lint": "bunx biome check .",
    "lint:fix": "bunx biome check --write .",
    "typecheck": "bunx tsc --noEmit",
    "postinstall": "npm install -g @github/copilot || echo 'Warning: Failed to install @github/copilot globally. You may need to run: npm install -g @github/copilot manually with appropriate permissions.'"
  },
  "dependencies": {
    "elysia": "^1.0.0",
    "@elysiajs/cors": "^1.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.4",
    "@types/bun": "^1.0.0",
    "typescript": "^5.3.0"
  }
}
